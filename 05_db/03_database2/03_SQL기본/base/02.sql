# 1.  titles 테이블의 데이터 출력하기
select
    *
from titles;

# 2. employees 테이블에서 first_name 컬럼만 출력
select first_name
from employees;

# 3. employees 테이블에서
# first_name 컬럼, last_name 컬럼, gender 컬럼 출력하기

select
    first_name, last_name, gender
from employees;